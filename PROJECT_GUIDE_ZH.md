# Black Dashboard React 專案使用指南 (正體中文)

這份指南將幫助您快速了解並開始開發基於 **Black Dashboard React** 的專案。

---

## 🚀 1. 專案簡介
**Black Dashboard React** 是一個美觀且功能齊全的 React 管理後台範本，由 Creative Tim 開發。它結合了 Bootstrap 4、Reactstrap 和 React 18，特別適合用於數據監控儀表板。

### 核心技術棧
*   **React**: 18.3.1 (使用 Hooks 與組件化開發)
*   **UI 框架**: Bootstrap 4.6.2 & [Reactstrap](https://reactstrap.github.io/)
*   **圖表庫**: [Chart.js](https://www.chartjs.org/) (透過 `react-chartjs-2` 封裝)
*   **導航路由**: React Router Dom v6
*   **樣式系統**: Sass (SCSS)

---

## 🛠️ 2. 快速開始

### 2.1 安裝依賴
確保您的電腦已安裝 Node.js，然後在專案根目錄執行：
```bash
npm install
```

### 2.2 啟動開發伺服器
執行以下命令啟動專案：
```bash
npm start
```
啟動後，請訪問 [http://localhost:3000](http://localhost:3000) 查看運行結果。

### 2.3 常用指令
*   `npm start`: 啟動開發環境。
*   `npm run build`: 構建生產環境版本（存放在 `/build` 目錄）。
*   `npm run compile-scss`: 手動編譯 SCSS 樣式文件到 CSS。
*   `npm run install:clean`: 清除 `node_modules` 並重新安裝（遇到套件問題時使用）。

---

## 📂 3. 目錄結構與關鍵文件

了解目錄結構有助於您快速開發：

```text
monitorreact/
├── public/              # 靜態資源 (index.html, favicon)
└── src/
    ├── assets/          # 樣式文件 (SCSS/CSS)、圖片、字體
    ├── components/      # 通用組件 (Sidebar, Navbar, Footer, FixedPlugin)
    ├── layouts/         # 佈局組件 (Admin.js, RTL.js)
    ├── variables/       # 常量配置 (例如圖表的數據與配置 charts.js)
    ├── views/           # 頁面內容 (Dashboard.js, UserProfile.js 等)
    ├── index.js         # 入口文件 (配置路由與全局樣式)
    └── routes.js        # 路由與側邊欄配置文件 (最重要！)
```

---

## 💡 4. 開發指引

### 4.1 如何新增一個頁面？
1.  在 `src/views/` 下建立新的組件文件，例如 `MyDashboard.js`。
2.  打開 `src/routes.js`，將新頁面加入 `routes` 陣列中：
    ```javascript
    {
      path: "/my-dashboard",
      name: "我的數據",
      icon: "tim-icons icon-chart-pie-36", // 圖標類名
      component: <MyDashboard />,
      layout: "/admin",
    }
    ```
3.  儲存後，側邊欄會自動顯示該選項。

### 4.2 如何更換圖標？
本專案內置了 **Nucleo Icons**。您可以在「Icons」頁面查看所有可用圖標，或在 `src/assets/css/nucleo-icons.css` 中尋找類名（通常以 `tim-icons` 開頭）。

### 4.3 切換深色與淺色模式
專案內建了背景切換功能。您可以透過點擊介面右側的「齒輪」圖標（FixedPlugin）來切換。在程式碼中，您可以透過修改 `Admin` layout 中的 body class 來預設模式。

---

## 📦 5. 部署
準備部署到正式環境時，請執行：
```bash
npm run build
```
構建後的內容將位於 `build/` 文件夾內，您可以將其部署到 Nginx、Vercel、Netlify 或任何靜態託管服務中。

---

## 🔗 6. 參考連結
*   [官方說明文件 (英文)](https://demos.creative-tim.com/black-dashboard-react/#/documentation/tutorial)
*   [Reactstrap 組件文檔](https://reactstrap.github.io/components/)
*   [Chart.js 官方文檔](https://www.chartjs.org/docs/latest/)

---
*Generated by Gemini CLI - 2026/04/02*
